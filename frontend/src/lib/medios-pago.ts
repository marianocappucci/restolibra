/** Shim: el hook vive en `libra-ui/comercio/medios-pago` desde P9-M3 (2026-09-06).
 *  Era byte a byte igual en Contalibra y Restolibra. Las pantallas que quedan en
 *  este producto lo siguen importando de aca. */
export { useMediosPago, useEtiquetaDeMedio, _resetCacheDeMedios, type MedioPago } from 'libra-ui/comercio/medios-pago'
