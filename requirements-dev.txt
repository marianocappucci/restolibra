# Dependencias de la suite de tests (no van a la imagen Docker).
# httpx ya esta en requirements.txt como dependencia runtime, pero se
# declara igual porque TestClient de Starlette lo requiere y esta suite
# no debe depender de ese detalle del runtime.
pytest>=8.0
httpx>=0.27.0
